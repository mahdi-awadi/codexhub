{
  "name": "codexhub",
  "version": "0.1.0-beta.2",
  "description": "Multi-session Codex hub — manage all your sessions from Telegram, Rubika, web, or CLI",
  "repository": "https://github.com/mahdi-awadi/codexhub",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "daemon": "bun run src/daemon.ts",
    "cli": "bun run src/cli.ts",
    "start": "bun run src/daemon.ts",
    "test": "bun test",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "build": "bun build src/daemon.ts --outfile dist/daemon.js --target bun --packages external && bun build src/cli.ts --outfile dist/cli.js --target bun --packages external"
  },
  "bin": {
    "hub": "./src/cli.ts",
    "hub-daemon": "./src/daemon.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "grammy": "^1.21.0",
    "playwright": "^1.48.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/bun": "latest"
  }
}
